spring:
  application:
    name: bank-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8083/realms/showcase-realm
server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka

info:
  app:
    name: ${spring.application.name}

#Actuator endpoints
management:
  endpoints:
    web:
      exposure:
        include: "*"
